
### Sample description

```{r}
#| label: samples
#| results: 'asis'
display_samplesheet <- es_data$samplesheet
if ("pool" %in% names(display_samplesheet)) {
  display_samplesheet <- add_pct_of_pool_to_samplesheet(
    display_samplesheet,
    es_data$pxl_data_processed
  )
}
print_metadata_table(display_samplesheet)
```
