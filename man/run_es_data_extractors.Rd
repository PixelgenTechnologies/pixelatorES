% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/es_data.R
\name{run_es_data_extractors}
\alias{run_es_data_extractors}
\title{Run registered \code{es_data} extractors}
\usage{
run_es_data_extractors(
  object,
  extractors = object$extractors,
  path = character()
)
}
\arguments{
\item{object}{An \code{es_data} object.}

\item{extractors}{Nested named list of functions. Defaults to
\code{object$extractors}. Used recursively for nested registries.}

\item{path}{Character vector of slot names from the root. Used recursively
to build diagnostic targets such as \code{"qc$crossing_edges"}.}
}
\value{
The updated \code{es_data} object.
}
\description{
Walks a nested named list of extractor functions depth-first. Each leaf must
be a function that takes the current \code{es_data} and returns the value for that
slot. Nested lists (e.g. under \code{qc}) fill nested slots such as
\code{object$qc$read_stats}.
}
\details{
An extractor may return an \code{es_data_extractor_result} to provide a partial
value with diagnostics. An unhandled extractor failure records a diagnostic
with \code{type = "extractor"} and leaves its destination slot \code{NULL} without
stopping the remaining extractors. Once \code{pxl_data_processed} is populated,
the raw \code{pxl_data} slot is cleared to release memory.
}
\keyword{internal}
